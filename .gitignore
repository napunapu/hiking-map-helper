# Generated by ElevationProfiler.groovy
*-profile.html
maps/

# Personal route data and its generated map cache - not tracked, kept local only
gpx/

# No longer used by ElevationProfiler.groovy (GraphHopper integration was
# replaced by the Overpass API, which needs no key), kept ignored in case a
# stray file with old key material is still present.
.env

# macOS Finder metadata, not source data
.DS_Store
